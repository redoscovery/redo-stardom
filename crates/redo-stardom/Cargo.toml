[package]
name = "redo-stardom"
version = "0.1.0"
edition = "2024"

[dependencies]
stardom-core = { path = "../stardom-core" }
bevy = "0.18"
bevy_egui = "0.39"
ron = "0.10"
serde = { version = "1", features = ["derive"] }
